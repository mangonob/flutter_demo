import 'package:flutter/material.dart';

class Layouts {
  Layouts._internal();

  factory Layouts() => Layouts._internal();

  Rect layoutAlignmentRect(
      {required Alignment alignment, required Rect rect, required offset}) {
    return Rect.zero;
  }
}
